자바의 4가지
- 클래스, 생성자, 변수, 메소드

메소드의 4가지
- 접근제한자, 리턴타입,파라미터,

기억할 메소드
- 이퀄스, 투트스링, 컴퍼레이터(컴페이투)

중요한것 4가지
-콜렉션 아이오 네트웍 쓰레드

클래스는 언제 쓰는가?
1. 변수 앞에 타입으로
2. 스테틱앞에 변수를 찾아갈때

- 변수 앞에 타입 타입때만 쓰나?
- 타입말고는?
- 스테틱 메소드나 변수일때, 클래스명.스테틱메소드 - 다른 메소드는 인스턴스를 만들어서 장효.메소드를 해야한다.
ex ) SYSTEM 은 클래스이다. 클래스명.스테틱메소드
    SYSTEM.OUT은 OUT은 SYSTEM 변수이다(?)

생성자는?
해당되는 클래스의 틀을 저장해놓고, 그 주소를 리턴할때 쓰는 것
스테틱이 아닌 요소들(변수,메소드)를 메모리에 저장해서 그 주소를 리턴하는데 쓰는것이 생성자이다
생성자의 최저 생계비 (디폴트 생성자)
클래스 생성시 생성자 구현안하면, 디폴트 생성자 있고, 구현하면 그 생성자이다.

변수는 그 클래스를 찾던지, 그 클래스가 저장되어있는 인스턴스를 찾던지이다.

System.out.print();
------ --  -------
클래스 변수 
	

스테틱 메소드는 장효.스테틱
그냥 메소드는 new 연산자로 만들고 난 후에 뜬다

임포트하면 그 클래스 쓸 수 있다.

메소드는 변수와 똑같다.
변수는 앞에 타입만 있지만 (접근제한자도), 메소드는 리턴타입,메소드명,파라미터가 있다.


피아노 프로그램 만들때
피아노 미디어 API를 가져온다.
그리고 자르를 쓴다.
자르는 클래스의 압축파일 - 자르를 빌트패스를 한다.
자르는 클래스 모임. 붙여놓고 ㄱ

















